@using App.Data.Entities
@model List<CategoryEntity>

@{
    ViewBag.Title = "Ürün Listesi";
}
<a href="@Url.Action("Admin","AdminDashboard")">Admin Paneli</a>
<h2>Kategori Listesi</h2>

@if (ViewBag.Error != null)
{
    <div class="alert alert-danger">
        @ViewBag.Error
    </div>
}

<table class="table">
    <thead>
        <tr>
            <th>Kategori adı</th>
            <th>Renk</th>
            <th>IconCssClass</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
         @foreach (var category in Model)
        {
            <tr>
                <td>@category.Name</td>
                <td>@category.Color</td>
                <td>@category.IconCssClass</td>
                <td>
                    <!-- Silme işlemi için form -->
                    <form method="post" action="@Url.Action("DeleteCategory", "Category")" 
                          onsubmit="return confirm('Bu ürünü silmek istediğinize emin misiniz?');">
                        <input type="hidden" name="id" value="@category.Id" />
                        <button type="submit" class="btn btn-danger">Sil</button>
                    </form>

                    |
                    <!-- Düzenle butonu -->
                    <a href="@Url.Action("UpdateCategory", "Category", new { id = category.Id })" class="btn btn-warning">Düzenle</a>
                </td>
            </tr>
        }
    </tbody>
</table>
